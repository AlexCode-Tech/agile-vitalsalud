{
  "name": "vitalsalud-monorepo",
  "version": "1.0.0",
  "description": "Sistema VitalSalud - Citas Médicas y CRM Clínico",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "cd frontend && npm install && npm run build",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.2.0",
    "mysql2": "^3.22.5",
    "node-cron": "^4.4.1",
    "nodemailer": "^9.0.1",
    "pg": "^8.22.0"
  }
}
